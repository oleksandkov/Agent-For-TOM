"""
Agent-For-TOM — Desktop GUI.

A CustomTkinter application that lets a teacher pick a document preset,
fill in the fields, and produce a PDF + DOCX in ДСТУ 3008:2015 style.
The AI call (HuggingFace Inference) and the PDF/DOCX renderers are
reused from the existing backend.
"""
