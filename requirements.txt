anthropic>=0.40.0
python-dotenv>=1.0.0
fpdf2>=2.7.0
playwright>=1.40.0
ddgs>=1.0.0
duckduckgo_search>=5.0.0
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0
pymupdf>=1.24.0
certifi>=2024.2.2
# Word automation, for skills/document-style-match/run.py's docx->pdf step.
# The agent's own convert_to_pdf comes from the word-docs MCP server, which a
# standalone script cannot reach; without this the verification gate stops
# before the PDF-side checks and has to ask for a tool call. Windows-only,
# which this project already is (the REPL is built on msvcrt).
pywin32>=306; sys_platform == "win32"
