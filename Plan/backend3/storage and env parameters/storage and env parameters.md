# in this file I will explain how to see the storage and what parmaters must be 


## storage and cache

The storage must contain the ouput from each session in the docx and pdf format, it must be availbe in GUI that the user can dowload it, remove, see from what session it is, when it wass cretead and otehr infor. 

Also in a storage of program must be the cache of all attached files which then have been compacted and saved with the current id to the database 

## env variables 


All env vairables must be able to see on the "Settings page" in the GUI of app, and here must bee an option to see/change/reomve/add/edit followig:

- the huggyface api key
- user style md file (must be an option to write it in the markdown format and preveiw)
- last sessoins with last outputs

And maybe other options which will be adeed later 