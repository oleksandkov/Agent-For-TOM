"""TOM MCP (Model Context Protocol) bridge — client + server lifecycle.

Implementation lands in Section 7. This package marker exists so the
import graph is stable from day one.
"""

__all__: list[str] = []
