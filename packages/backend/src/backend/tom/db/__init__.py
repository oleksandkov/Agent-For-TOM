"""TOM data storage layer (SQLite + SQLCipher + sqlite-vec + OS keyring).

Implementation lands in Section 3. This package marker exists so the
import graph is stable from day one.
"""

__all__: list[str] = []
