"""TOM HTTP API surface (chat, sessions, memory, skills, improvement).

Implementation lands in Section 6. This package marker exists so the
import graph is stable from day one.
"""

__all__: list[str] = []
